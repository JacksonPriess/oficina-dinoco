apiVersion: v1
kind: ConfigMap
metadata:
  name: oficina-api-config
  labels:
    app: oficina-api
data:
  SPRING_PROFILES_ACTIVE: "dev"
  OPENAPI_SERVER_URL: "https://wgpvt7ytj0.execute-api.us-east-1.amazonaws.com"

  # DB_ENDPOINT será substituído pela pipeline durante o deploy
  SPRING_DATASOURCE_URL: "jdbc:postgresql://${DB_ENDPOINT}:5432/oficina_db"

  TZ: "America/Sao_Paulo"