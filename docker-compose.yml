services:
  postgres:
    image: postgres:16-alpine
    container_name: oficina-postgres
    environment:
      POSTGRES_DB: oficina_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: adminpassword
      TZ: America/Sao_Paulo
    ports:
      - "5433:5432"
    volumes:
      - oficina_pg_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U admin -d oficina_db" ]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: oficina-api
    ports:
      - "8080:8080"
    environment:
      # 2. Sobrescrevemos as variáveis do application.properties para usar a rede do Docker
      SPRING_PROFILES_ACTIVE: dev
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/oficina_db
      SPRING_DATASOURCE_USERNAME: admin
      SPRING_DATASOURCE_PASSWORD: adminpassword
      TZ: America/Sao_Paulo
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

volumes:
  oficina_pg_data: